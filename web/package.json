{
  "name": "polynesie-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "sync": "mkdir -p public && cp ../*.html ../style.css public/",
    "predev": "npm run sync",
    "dev": "next dev",
    "prebuild": "npm run sync",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "@vercel/postgres": "^0.10.0",
    "next": "^15.4.6",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  }
}
